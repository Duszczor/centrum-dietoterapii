/**
 * Shared bootstrap constants for front-end app initialization.
 */
(function setupBootstrapConstants() {
  window.DietitianBootstrap = window.DietitianBootstrap || {};

  window.DietitianBootstrap.constants = {
    HEADER_EXTRA_OFFSET: 16,
    BREAKPOINT_MD_FALLBACK: "768px",
  };
})();
